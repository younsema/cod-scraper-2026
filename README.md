# 📦 أداة سحب بيانات المنتجات (COD Morocco Scraper)

أداة ويب كتمكنك تسحب بيانات المنتجات (العنوان، الثمن، الصورة، الرابط)
من متاجر Shopify و YouCan فـ أقل من دقيقة، وتصدرها كملف Excel أو CSV.

---

## 🆕 شنو تبدل فهاد النسخة (تحسينات)

**إصلاحات مهمة:**
- 🐛 **باگ مصلح**: قبل، إلا لصقتي رابط منتج واحد ديال Shopify، الأداة كانت
  كتسحب المتجر كامل بدل المنتج لي بغيتي بالضبط. دابا كتفرق بين رابط منتج
  واحد (`/products/handle`) ورابط متجر/كولكسيون.
- 🔒 **`max_uses` دابا كيخدم فعليا**: قبل كان مكتوب فـ secrets ولكن ماشي
  مطبق. دابا `usage_tracker.py` كيتبع عدد المحاولات ديال كل كود تجريبي
  ويمنعو إلا خلاص الحد المسموح.
- ⚡ **أسرع**: `requests.Session` وحدة معاد استعمالها + retry أوتوماتيكي
  عند فشل الطلب (timeout, 5xx) بدل ما توقف الأداة مباشرة.
- 🧹 **دون تكرار**: المنتجات المكررة (نفس الرابط) كتفلتر أوتوماتيكيا.

**ميزات جديدة:**
- 📊 لوحة إحصائيات (عدد المنتجات، متوسط/أرخص/أغلى ثمن) + رسم بياني لتوزيع الأثمنة
- 🔍 بحث وترتيب فـ الجدول قبل التصدير
- ⬇️ تصدير Excel **و** CSV
- 🛡️ حماية بسيطة ضد محاولات تخمين الكود (قفل مؤقت بعد 5 محاولات فاشلة)
- 🏷️ الشريط الجانبي كيبين نوع الكود، الأيام الباقية، وعدد المحاولات المتبقية
- 🎨 تصميم أوضح (خط Tajawal، بطاقات إحصائيات، badges لنوع الكود)

---

## 1) التشغيل محلياً (على جهازك) قبل النشر

```bash
# 1. ثبت المتطلبات
pip install -r requirements.txt

# 2. سمي secrets.toml.example ب secrets.toml
cp .streamlit/secrets.toml.example .streamlit/secrets.toml

# 3. بدل الأكواد بالأكواد ديالك فـ .streamlit/secrets.toml

# 4. شغل التطبيق
streamlit run app.py
```

غادي يفتح فـ المتصفح على `http://localhost:8501`

---

## 2) النشر المجاني 100% (GitHub + Streamlit Cloud)

### الخطوة أ: رفع الكود لـ GitHub

1. أنشئ حساب فـ [github.com](https://github.com) إلا ماعندكش.
2. أنشئ Repository جديد (يقدر يكون **Public**، لا مشكل، لأن الأكواد السرية
   ماغاديش تكون فـ الكود بفضل نظام secrets).
3. ارفع ليه هاد الملفات:
   - `app.py`
   - `scraper.py`
   - `usage_tracker.py`
   - `requirements.txt`
   - `.gitignore`
   - `data/.gitkeep` (باش يبقى المجلد موجود بلا ما نرفعو usage.json الحقيقي)
   - **ملا ترفعش** `.streamlit/secrets.toml` (الملف الحقيقي فيه الأكواد ديال الزبناء)
   - **ملا ترفعش** `data/usage.json` (كيتخلق أوتوماتيكيا، فيه العدادات)

```bash
git init
git add app.py scraper.py usage_tracker.py requirements.txt .gitignore README.md data/.gitkeep
git commit -m "أول نسخة من الأداة"
git branch -M main
git remote add origin https://github.com/USERNAME/REPO_NAME.git
git push -u origin main
```

### الخطوة ب: الربط مع Streamlit Cloud

1. دخل لـ [share.streamlit.io](https://share.streamlit.io)
2. اربط حساب GitHub ديالك.
3. اختار الـ Repository لي رفعتي، والملف الرئيسي: `app.py`
4. قبل ما تدوس Deploy، دوس على **Advanced settings > Secrets** وحط:

```toml
[keys]
"TEST-FREE-00" = { type = "trial", expires = "2026-12-31", max_uses = 20 }
"CLIENT-AHMED-01" = { type = "paid", expires = "", max_uses = 0 }
```

5. دوس **Deploy**. بعد دقيقة أو دقيقتين غادي يعطيك رابط من نوع:
   `https://your-app-name.streamlit.app`

هاد الرابط هو لي غادي تبعتو للزبناء ديالك.

---

## 3) كيفاش تزيد / تحذف كود تفعيل لزبون جديد (بلا ماتلمس الكود)

1. دخل لـ [share.streamlit.io](https://share.streamlit.io) > التطبيق ديالك
2. Settings > Secrets
3. زيد سطر جديد بهاد الشكل:

```toml
"CLIENT-FATIMA-02" = { type = "paid", expires = "", max_uses = 0 }
```

4. Save. التطبيق غادي يتحدث لايف فـ ثوانٍ، بلا ما تعاود تدير Deploy.

**لحذف كود**: غير امسح السطر ديالو من الـ Secrets و Save.

**لكود تجربة بتاريخ محدود**: حط `expires = "2026-08-15"` مثلا، والكود
غادي يخرج من الخدمة أوتوماتيكيا بعد هاد التاريخ.

**لكود تجربة بعدد محاولات محدود**: حط `max_uses = 10` مثلا، والكود غادي
يتوقف أوتوماتيكيا بعد 10 عمليات سحب (العداد فـ `data/usage.json`).

> ⚠️ **حد مهم**: عداد `max_uses` كيتخزن فـ ملف محلي على الـ instance ديال
> Streamlit Cloud. إلا التطبيق تعاود عليه deploy، أو نام بعد فترة بلا استعمال
> وتفاق (cold start)، العداد يمكن يترجع لصفر. هادشي كافي لغالبية الحالات
> (تجربة مجانية محدودة)، ولكن إلا بغيتي عداد دائم 100% حتى مع النوم/الـ redeploy،
> خاصك تربطو بقاعدة بيانات خارجية (Supabase, Google Sheets API, Firebase...).

---

## 4) كيفاش تخدم الأداة

1. الزبون كيدخل الرابط ديال الموقع (`*.streamlit.app`)
2. كيدخل كود التفعيل ديالو
3. كيلصق رابط منتج واحد، أو رابط متجر/كاتيغوري كامل
4. الأداة كتسحب البيانات وتعرضها فـ جدول، مع إحصائيات وبحث/ترتيب
5. زر "Excel" ولا "CSV" كيعطيه الملف جاهز للتحميل

### أنواع الروابط المدعومة

| نوع الرابط | كيفاش كتخدم |
|---|---|
| رابط منتج Shopify واحد (`/products/handle`) | كتسحب JSON-LD ديال هاد المنتج بالضبط |
| رابط متجر Shopify كامل / كولكسيون | كتستعمل `/products.json` (الأسرع، كيجيب المتجر كامل) |
| رابط منتج YouCan واحد | كتسحب meta tags / JSON-LD |
| رابط كاتيغوري/متجر YouCan | كتلقط روابط المنتجات وتزورهم واحد واحد |

---

## 5) ملاحظات مهمة

- **الأداء**: سحب متجر كامل (100+ منتج) يقدر ياخد بضع دقائق حسب سرعة الموقع
  المستهدف، لأن الأداة كتحترم delay بين الطلبات باش ما تبانش كـ هجوم.
- **الحماية القانونية**: الأداة كتسحب غير المعلومات العمومية المعروضة للزوار
  (العنوان، الثمن، الصورة)، وماكتدخلش لأي حساب أو معلومات خاصة.
- **حدود Streamlit Cloud المجاني**: التطبيقات المجانية عندها حدود فـ الموارد
  (RAM/CPU)، إلا كبر عدد المستخدمين بزاف يمكن تحتاج نسخة مدفوعة أو استضافة
  أخرى (Render, Railway...).
- **تخزين العدادات**: شوف الملاحظة فـ القسم 3 بخصوص حدود `data/usage.json`.

---

## 6) البنية التقنية

```
cod-scraper-app/
├── app.py                          # الواجهة + نظام الحماية + الإحصائيات + التصدير
├── scraper.py                      # منطق سحب البيانات (Shopify + YouCan)
├── usage_tracker.py                # تتبع وتحديد عدد استعمالات الأكواد التجريبية
├── requirements.txt                # المكتبات المطلوبة
├── .gitignore                      # باش ما نرفعوش secrets.toml أو usage.json الحقيقيين
├── data/
│   └── .gitkeep                    # مجلد فارغ، usage.json كيتخلق أوتوماتيكيا هنا
└── .streamlit/
    └── secrets.toml.example        # مثال، ديرو نسخة بلا .example محليا
```
